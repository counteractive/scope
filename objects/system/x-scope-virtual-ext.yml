# this is a stub and does not yet reflect a complete proposed custom object
# schema

# The Virtual System extension covers virtual machines (VMs), containers,
# function-as-a-service runtimes, etc. It would be useful for storing 
# hypervisor information, for example.